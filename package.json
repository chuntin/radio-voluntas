{
  "name": "radio",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "node -e \"const fs=require('fs');const f='public/sw.js';const s=fs.readFileSync(f,'utf8');const m=s.match(/voluntas-radio-v(\\d+)/);const v=m?parseInt(m[1])+1:1;fs.writeFileSync(f,s.replace(/^const CACHE_NAME = .*/m,\\\"const CACHE_NAME = 'voluntas-radio-v\\\"+v+\\\"';\\\"));\" && cp src/assets/db.json public/db.json && cp dist/index.html dist/404.html && cp editor-db.html dist/editor-db.html",
    "predeploy": "npm run build",
    "deploy": "npm run build && gh-pages -d dist --dotfiles"
  },
  "dependencies": {
    "vue": "^3.0.4",
    "vue-router": "^4.1.6"
  },
  "devDependencies": {
    "@vue/compiler-sfc": "^3.0.4",
    "gh-pages": "^6.3.0",
    "mediaelement": "^4.2.8",
    "sass": "^1.59.3",
    "sass-loader": "^13.2.1",
    "vite": "^1.0.0-rc.13"
  }
}
